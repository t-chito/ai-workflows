# promptfoo コードレビュー評価

## 使い方

```bash
# インストール
npm install

# 認証
claude login
claude setup-token
export CLAUDE_CODE_OAUTH_TOKEN="表示されたトークン"
codex auth

# 実行
npx promptfoo eval
npx promptfoo view
```
